import { ifEq } from 'summer/helpers/if-eq';
import { module, test } from 'qunit';

module('Unit | Helper | if eq');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifEq([42]);
  assert.ok(result);
});
