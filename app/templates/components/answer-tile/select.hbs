{{#x-select value=tag.answer action="selectOption" class="form-control"}}
  {{#each question.options as |option|}}
    {{#x-option value=option.value selected=option.isSelected}}{{option.text}}{{/x-option}}
  {{/each}}
{{/x-select}}
